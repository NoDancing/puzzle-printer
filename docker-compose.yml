services:
  puzzle-printer:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - TZ=America/New_York
    volumes:
      - ./logs:/var/log/puzzle-printer
      - ~/.ssh/puzzle-site.pem:/run/secrets/puzzle-site.pem:ro
